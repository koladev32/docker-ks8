apiVersion: apps/v1
kind: Deployment
metadata:
  name: express-node-app
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: express-node-app
  template:
    metadata:
      labels:
        app: express-node-app
    spec:
      containers:
      - name: express-node-app
        image: koladev32/express-image
        imagePullPolicy: Always
        ports:
        - containerPort: 5005
---
apiVersion: v1
kind: Service
metadata:
  name: express-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: express-node-app
  ports:
    - port: 5005
      targetPort: 5005  
